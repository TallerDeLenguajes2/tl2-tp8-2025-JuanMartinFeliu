@model Presupuestos.Presupuesto

<section class="container mt-4">
    <h2 class="text-center text-danger">Eliminar Presupuesto</h2>

    <div class="alert alert-danger mt-3">
        ¿Seguro que deseas eliminar el presupuesto <strong>@Model.IdPresupuesto</strong> 
        perteneciente a <strong>@Model.NombreDestinatario</strong>?
    </div>

    <form asp-action="Delete" method="post">
        <input type="hidden" asp-for="IdPresupuesto" />
        <button type="submit" class="btn btn-danger">Eliminar</button>
        <a asp-action="Index" class="btn btn-secondary">Cancelar</a>
    </form>
</section>
